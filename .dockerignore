# VCS
.git
.github

# IDE / editor
.idea
.vscode

# Dependencies (reinstalled inside the image)
vendor

# Build output / caches (regenerated by the test suite)
tests/_output
**/.phpunit.result.cache
coverage
coverage.xml

# Docker / compose (not needed inside the image)
docker-compose.yml
.dockerignore
